name: test

# Gates run on every push to master, main, and macos-port, and on PRs.
# master mirrors the upstream project; main/macos-port are the personal-fork
# line. Mutation testing is a local discovery tool (cargo mutants --file
# <path>), not a CI gate. The release workflow still fires on tag push.
on:
  push:
    branches: [master, main, macos-port]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

# Cancel superseded runs on the same ref; each run builds ~200 test binaries.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: clippy (gate)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config
      - name: Gating clippy run
        run: cargo clippy --all-targets --all-features --locked

  clippy_audit:
    name: clippy (aspirational, non-gating)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config
      - name: Pedantic + nursery + missing-docs warning count
        run: cargo clippy --all-targets --all-features --locked -- -W clippy::pedantic -W clippy::nursery -W missing_docs

  deny:
    name: cargo-deny (advisories + licenses + bans)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
          arguments: --all-features

  machete:
    name: cargo-machete (unused deps)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: bnjbvr/cargo-machete@main

  unwrap_check:
    name: forbid unwrap/expect in production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config
      - name: Clippy unwrap_used / expect_used on non-test build
        run: |
          cargo clippy --lib --bins --all-features --locked \
            -- -D clippy::unwrap_used -D clippy::expect_used

  test:
    name: nextest + mpv
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-nextest
      - name: Install runtime deps (mpv for real-mpv smoke test)
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends mpv libdbus-1-dev pkg-config
      # ci profile: retries=2 so a subprocess/timing test that's known-flaky
      # under parallel CI load (per CLAUDE.md) retries instead of failing the
      # gate; a genuine break still fails all attempts.
      - name: Run tests
        run: cargo nextest run --profile ci --all-targets --locked
      - name: Doc tests (nextest skips these)
        run: cargo test --doc --locked

  # macOS build/clippy coverage lives in macos-release.yml, which builds the
  # app for x86_64-apple-darwin and runs a lib+bins clippy pass on every push
  # to main/macos-port with caching. The previous report-only build_macos
  # matrix here duplicated that build with a much heavier --all-targets
  # clippy on the same Intel runners, so it was folded away.

  coverage:
    name: coverage (report only)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov,cargo-nextest
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends mpv libdbus-1-dev pkg-config
      # nextest backend = process-per-test, matching the gating test job. The
      # plain `cargo test` backend shares one process, so tests that set the
      # process-global FERROSONIC_CONFIG_DIR env race and crash a spawned daemon.
      # The coverage profile also drops real-binary e2e tests (no in-process
      # coverage). best-effort: a test that flakes only under llvm-cov
      # instrumentation must not block the report, which the gating test job
      # already covers; profraw from every test that ran is still written.
      - name: Collect coverage (nextest backend, coverage profile)
        continue-on-error: true
        run: NEXTEST_PROFILE=coverage cargo llvm-cov nextest --all-features --workspace --locked --no-report
      - name: Generate lcov
        run: cargo llvm-cov report --lcov --output-path lcov.info
      - name: Generate HTML report
        run: cargo llvm-cov report --html
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage-html
          path: target/llvm-cov/html/
          retention-days: 30
      - name: Print coverage summary
        run: cargo llvm-cov report --summary-only
